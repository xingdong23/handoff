---
allowed-tools: Bash(handoff:*)
description: Recompute Handoff reminders for the current project
---

Recompute reminders from Capsules, Git status, and GitLab state.

Run:

```bash
handoff reminders scan "$ARGUMENTS"
```
