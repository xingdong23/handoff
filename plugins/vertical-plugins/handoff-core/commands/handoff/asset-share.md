---
allowed-tools: Bash(handoff:*)
description: 分享 Handoff 统一资产
---

分享 `$ARGUMENTS` 指定的资产。参数可以是 Capsule id、Knowledge Capsule id 或 Skill Asset id。

执行：

```bash
handoff asset share "$ARGUMENTS"
```

Skill Asset 需要先通过审核。
