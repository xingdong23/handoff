---
allowed-tools: Bash(handoff:*)
description: Create a share link for a Handoff Capsule
---

Create a share link for the Capsule named in `$ARGUMENTS`.

Run:

```bash
handoff share "$ARGUMENTS"
```

Return the share token and local URL.
