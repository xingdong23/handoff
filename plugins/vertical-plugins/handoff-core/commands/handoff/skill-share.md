---
allowed-tools: Bash(handoff:*)
description: 分享审核通过的 Handoff Skill Asset
---

分享 `$ARGUMENTS` 指定的 Skill Asset。Skill Asset 需要先通过审核。

执行：

```bash
handoff skill share "$ARGUMENTS"
```

返回分享 token、本地页面地址和 API 地址。团队成员可以使用分享地址或 token 导入。
