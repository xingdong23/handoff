---
allowed-tools: Bash(handoff:*)
description: Delete a Handoff Capsule
---

Delete the Capsule named in `$ARGUMENTS`.

Run:

```bash
handoff delete "$ARGUMENTS"
```

Return the deleted Capsule id and title.
