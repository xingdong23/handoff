---
allowed-tools: Bash(handoff:*)
description: Open the Handoff dashboard
---

Open the Handoff dashboard for all captured projects.

Run:

```bash
handoff open "$ARGUMENTS"
```

Return the dashboard URL.
