---
allowed-tools: Bash(handoff:*)
description: Show the Handoff dashboard status in the terminal
---

Show the current Handoff dashboard status across all projects.

Run:

```bash
handoff status "$ARGUMENTS"
```
